---
description: "빌드 실패 또는 TypeScript 컴파일 에러 발생 시 사용. npm run build 에러를 파일별로 분석하고 한 번에 하나씩 수정. 3회 시도 실패 시 중단."
---

# Build Fix

build-error-resolver 에이전트를 호출하여 빌드 에러를 점진적으로 수정한다.

1. 빌드 실행 → 에러 분석 (파일별 그룹화)
2. 한 번에 하나의 에러만 수정
3. 수정 후 빌드 재실행 → 확인
4. 동일 에러 3회 실패 시 중단, 새 에러 발생 시 중단
